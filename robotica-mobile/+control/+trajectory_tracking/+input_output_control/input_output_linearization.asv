function [v,w] = input_output_linearization( xstar, xdotstar, x, ...
                                             ystar, ydotstar, y, ...
                                             theta, thetastar, ...
                                             vstar, omegastar)

    
    xBdotstar = xdotstar;
    yBdotstar = ydotstar;
    
    u1 = xBdotstar + K1*(xstar-xB);
    u2 = yBdotstar + K2*(ystar-yB);

    T = [cos(theta) -b*sin(theta);
         sin(theta) b*cos(theta)];
    invT = inv(T);
    
    
end