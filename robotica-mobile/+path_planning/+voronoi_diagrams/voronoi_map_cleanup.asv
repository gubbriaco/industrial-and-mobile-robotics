function [vxc,vyc] = voronoi_map_cleanup(vx, vy, nr_obstacles, obstacles)

    for i=length(vx):-1:1
        x_line = vx(:,i);
        y_line = vy(:,i);
        
        for j=1:nr_obstacles
            x_obst = obstacles(j,1);
            y_obst = obstacles(j,2);
            w_obst = obstacles(j,3);
            h_obst = obstacles(j,4);
            
            if 
        end
    end

end